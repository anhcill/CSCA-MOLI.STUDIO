# One-Off Backend Scripts

Thư mục này chứa các script backend chạy một lần để kiểm tra hoặc sửa dữ liệu cũ.

Các script cần kết nối database phải dùng `DATABASE_URL` từ `backend/.env`, không hard-code connection string vào file.
