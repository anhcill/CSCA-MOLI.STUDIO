import Link from 'next/link';
import type { CurriculumSectionDto } from '@/lib/types/courses';

export function LearningSidebar({ courseSlug, sections, activeLessonId }: { courseSlug: string; sections: CurriculumSectionDto[]; activeLessonId: number }) {
  return <aside aria-label="Nội dung khóa học" className="h-full overflow-y-auto border-l border-slate-200 bg-white text-slate-950 dark:border-slate-800 dark:bg-slate-900 dark:text-slate-100"><div className="border-b border-slate-200 p-5 dark:border-slate-800"><h2 className="text-xl font-black">Nội dung khóa học</h2></div>{!sections.length ? <p className="p-6 text-sm text-slate-500 dark:text-slate-400">Chưa có bài học.</p> : sections.map((section, index) => <section key={section.id}><h3 className="border-b border-slate-100 bg-slate-50 px-5 py-4 font-black dark:border-slate-800 dark:bg-slate-950/70">{index + 1}. {section.title}</h3>{section.lessons.map((item) => item.isLocked ? <div key={item.id} className="border-b border-slate-100 px-5 py-4 text-sm text-slate-400 dark:border-slate-800 dark:text-slate-500">🔒 {item.title}</div> : <Link key={item.id} href={`/hoc/${courseSlug}/bai-hoc/${item.id}`} aria-current={item.id === activeLessonId ? 'page' : undefined} className={`block border-b border-slate-100 px-5 py-4 text-sm dark:border-slate-800 ${item.id === activeLessonId ? 'bg-indigo-50 font-bold text-indigo-700 dark:bg-indigo-950/50 dark:text-indigo-300' : 'text-slate-700 hover:bg-slate-50 dark:text-slate-300 dark:hover:bg-slate-800'}`}>{item.progressStatus === 'completed' ? '✓ ' : ''}{item.title}</Link>)}</section>)}</aside>;
}
