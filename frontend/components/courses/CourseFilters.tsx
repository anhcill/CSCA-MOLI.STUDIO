'use client';

import type { CourseAccessType, CourseCatalogQuery, CourseLevel, CscaSubjectCode } from '@/lib/types/courses';

export function CourseFilters({ value, onChange }: { value: CourseCatalogQuery; onChange: (next: CourseCatalogQuery) => void }) {
  const update = (key: keyof CourseCatalogQuery, nextValue: string) => onChange({ ...value, [key]: nextValue || undefined, page: 1 });
  return <div className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 sm:grid-cols-2 lg:grid-cols-4"><select aria-label="Môn học" value={value.subjectCode || ''} onChange={(event) => update('subjectCode', event.target.value as CscaSubjectCode)} className="rounded-xl border border-slate-200 px-3 py-2"><option value="">Tất cả môn CSCA</option><option value="MATH">Toán</option><option value="PHYSICS">Vật lý</option><option value="CHEMISTRY">Hóa học</option><option value="CHINESE_SCI">Tiếng Trung Tự nhiên</option><option value="CHINESE_SOC">Tiếng Trung Xã hội</option></select><select aria-label="Quyền truy cập" value={value.accessType || ''} onChange={(event) => update('accessType', event.target.value as CourseAccessType)} className="rounded-xl border border-slate-200 px-3 py-2"><option value="">Mọi quyền truy cập</option><option value="free">Miễn phí</option><option value="vip">VIP</option><option value="premium">PRE</option><option value="contact">Liên hệ</option></select><select aria-label="Trình độ" value={value.level || ''} onChange={(event) => update('level', event.target.value as CourseLevel)} className="rounded-xl border border-slate-200 px-3 py-2"><option value="">Mọi trình độ</option><option value="basic">Cơ bản</option><option value="intermediate">Trung cấp</option><option value="advanced">Nâng cao</option></select><select aria-label="Sắp xếp" value={value.sort || 'newest'} onChange={(event) => update('sort', event.target.value)} className="rounded-xl border border-slate-200 px-3 py-2"><option value="newest">Mới nhất</option><option value="popular">Phổ biến</option><option value="rating">Đánh giá cao</option></select></div>;
}
